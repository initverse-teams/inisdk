name: "Install"
description: "Install dependencies"

runs:
  using: "composite"
  steps:
    - name: Set up foundry
      uses: foundry-rs/foundry-toolchain@v1

    - name: Set up pnpm
      uses: initverse-teams/actions/.github/actions/pnpm@pnpm-10.4.1