name: "Install"
description: "Install dependencies"

runs:
  using: "composite"
  steps:
    - name: Set up foundry
      uses: foundry-rs/foundry-toolchain@v1

    - name: Set up pnpm
      uses: ./.github/actions/pnpm